<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  thinlet-modernized Checkstyle config.

  Derived from Google's style, deliberately RELAXED to fit the 2005 legacy
  source without production-code edits (see DECISIONS.md, plan Phase 0 step 6):
    - LineLength max raised to 120 to match palantir-java-format's column limit.
    - Star imports allowed (Thinlet.java uses java.awt.* etc.; google-java-format
      does not expand them).
    - Member / parameter / local naming checks omitted (legacy short names like
      c, k, v are intentional until Phase 3 internal refactors).
  Tightened over time; Phase 0 priority is "pass on the existing file."
-->
<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>
  <property name="fileExtensions" value="java, properties, xml"/>

  <!-- Suppressions file (per-path relaxations). Path is injected by the Maven
       plugin's <suppressionsLocation> via the checkstyle.suppressions.file
       property. -->
  <module name="SuppressionFilter">
    <property name="file" value="${checkstyle.suppressions.file}" default=""/>
    <property name="optional" value="true"/>
  </module>

  <!-- File-level checks.
       FileTabCharacter is intentionally omitted: the 2005 source carries
       tab-indented code inside block comments (e.g. the commented-out wrap()
       method in Thinlet.java); palantir correctly leaves comment interiors
       untouched, so a tab check would fire on comment content we preserve. -->
  <module name="NewlineAtEndOfFile">
    <property name="lineSeparator" value="lf"/>
  </module>
  <module name="LineLength">
    <property name="max" value="120"/>
    <property name="ignorePattern" value="^package.*|^import.*|href|http://|https://"/>
  </module>

  <module name="TreeWalker">
    <!-- Required to honor SuppressionCommentFilter. -->
    <module name="SuppressionCommentFilter"/>

    <!-- Imports: star imports intentionally NOT forbidden here. -->
    <module name="RedundantImport"/>
    <module name="UnusedImports">
      <property name="processJavadoc" value="true"/>
    </module>

    <!-- Type naming (relaxed set — types only, not members). -->
    <module name="TypeName"/>
    <module name="PackageName"/>

    <!-- Blocks / braces.
         NeedBraces, EmptyBlock, and MissingSwitchDefault are intentionally
         omitted for Phase 0: the unmodified 2005 source uses brace-less single
         statements, an intentional empty-if side-effect call, and
         default-less switches. palantir-java-format preserves these, and we do
         not edit production code to satisfy the linter (plan Phase 0 step 6).
         Revisit during Phase 3 internal refactors. -->
    <module name="LeftCurly"/>

    <!-- Correctness smells that pass on the palantir-formatted legacy source. -->
    <module name="FallThrough"/>
    <module name="OneStatementPerLine"/>
    <module name="UpperEll"/>
    <module name="ModifierOrder"/>
  </module>
</module>
