# Input surface

> **Stub — populated from Phase 1 trace output.**

The events Thinlet consumes from AWT (mouse, key, focus, wheel, timer), with
browser-equivalent mappings noted for a future portable backend.

<!-- Populated by the trace-curator agent from input-event trace data once the
     Phase 1 tracing harness lands. -->
