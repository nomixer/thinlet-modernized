# Layout algorithms

> **Stub — populated from Phase 1 trace output.**

Each layout pass (grid, box, border, absolute): inputs, outputs, and AWT
touchpoints, sourced from `LayoutTrace` entry/exit logging.

<!-- Populated by the trace-curator agent from LayoutTrace output once the
     Phase 1 tracing harness lands. -->
